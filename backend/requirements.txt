FROM python:3.9-slim-bullseye

USER root
ENV DEBIAN_FRONTEND=noninteractive

# 1. Install EVERYTHING needed to build AI software from scratch
RUN apt-get update -y && \
    apt-get install -y --no-install-recommends \
    libgl1 \
    libglib2.0-0 \
    gcc \
    g++ \
    python3-dev \
    git \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# 2. Install Torch (CPU version)
RUN pip install --no-cache-dir torch torchvision --index-url https://download.pytorch.org/whl/cpu

# 3. Install Detectron2 directly from GitHub (The most reliable way)
# This avoids broken links and builds it specifically for your server
RUN pip install --no-cache-dir 'git+https://github.com/facebookresearch/detectron2.git'

# 4. Standard requirements
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# 5. Finish setup
COPY . /app
EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]